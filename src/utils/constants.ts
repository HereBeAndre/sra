export const EURO_TO_DOLLAR_EXCHANGE_RATE = 1.1319586;
export const OPENWEATHERAPI_TEMPERATURE_UNITS = 'metric';
export const STANDARD_DATE_FORMAT = 'DD-MM-YYYY';

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export enum EGetValueFromData {
  NOT_AVAILABLE = 'Not Available',
  EMPTY_STRING = '',
}

export enum EStorageData {
  NAVIGATION_LINK = 'navigationLink',
  TASK_LIST = 'taskList',
}
